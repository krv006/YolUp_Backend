# 6.1'da django.utils.cache.cc_delim_re olib tashlangan — DRF hali mos emas,
# shuning uchun 6.0.x ga qotirilgan (keshsiz build'da sinov o'tmagan versiya tushmasin)
Django>=6.0,<6.1
djangorestframework>=3.17
djangorestframework-simplejwt>=5.5
django-cors-headers>=4.9
drf-spectacular>=0.30
django-filter>=25.1
python-dotenv>=1.2
psycopg[binary]>=3.3
livekit-api>=1.2
gunicorn>=23.0
whitenoise>=6.8
# channels_redis 4.x redis-py 8.x bilan mos emas — blocking (BRPOP) o'qishda
# soxta TimeoutError beradi (WebSocket consumer'lar tinimsiz yiqiladi).
# 5.x qatorida qotirilgan — sinovdan o'tgan, barqaror kombinatsiya.
redis>=5.0,<6.0
reportlab>=4.2
sympy>=1.13
matplotlib>=3.8
django-jazzmin>=3.0
google-generativeai>=0.8
python-docx>=1.1
openpyxl>=3.1
nh3>=0.2
Pillow>=11.0
channels>=4.1
channels-redis>=4.2
daphne>=4.1
uvicorn[standard]>=0.30
pywebpush>=2.0
psutil>=6.0
# Google Docs'dan ochiq (public) eksport orqali test import qilish uchun —
# oldin faqat tranzitiv bog'liqlik edi (google-generativeai orqali), endi
# to'g'ridan-to'g'ri ishlatilgani uchun aniq belgilandi.
requests>=2.33
